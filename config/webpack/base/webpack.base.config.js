const shared = require('./webpack.base.shared.config')
const server = require('./webpack.base.server.config')
const client = require('./webpack.base.client.config')

module.exports = {
  shared,
  server,
  client
}
